# DISABLED: GitHub Pages deployment was replaced by Vercel deployment.
# Vercel is required to run Next.js API routes (Stripe webhooks, booking checkout,
# email sending, reminder cron). Vercel auto-deploys on every push to main.
#
# This workflow is kept (manual-only trigger) for historical reference but does nothing.
# To permanently remove, delete this file.

name: (DISABLED) Deploy Next.js site to Pages

on:
  workflow_dispatch:

jobs:
  disabled-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment moved to Vercel
        run: echo "Site deployment has moved to Vercel. This workflow is disabled."
